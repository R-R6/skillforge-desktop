---
description: 修改代码后自动重启开发环境；推送远程时使用本地 HTTP 代理
alwaysApply: true
---

# 自动重启开发环境

在本项目（skillforge-desktop）完成代码修改后，**必须**由 Agent 自动重启应用，不要让用户手动操作。

## 执行步骤

1. 若已有 dev 进程在运行，先结束旧的 Electron 进程再重启。
2. 优先使用 `npm run dev`（electron-vite 热重载）；若只改了 main/preload 或 dev 未运行，则先 `npm run build` 再启动 dev。
3. 在后台启动 dev，确认终端无致命错误后再结束本轮回复。
4. 回复中简要说明应用已重启，**不要**写「请手动重启」。

## 命令参考（PowerShell）

```powershell
Get-Process -Name "electron","SkillForge Desktop" -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
cd "f:\project\myself\skillforge-desktop"
npm run dev
```

# 推送远程仓库

在本项目向远程仓库执行 `git push` 时，**必须**走本地 HTTP 代理 `http://127.0.0.1:7897`，不要裸连 GitHub。

## 执行步骤

1. 先完成 commit（用户明确要求推送时再提交）。
2. 推送时使用 `-c http.proxy=...`（GitHub 为 HTTPS，同时带上 `https.proxy`）。
3. 若仍失败，可改用环境变量：`https_proxy=http://127.0.0.1:7897 git push ...`。
4. 推送成功后简要告知远程分支与 commit，**不要**让用户手动配置代理。

## 命令参考

macOS / Linux：

```bash
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
```

PowerShell：

```powershell
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
```
